# NOTE: torch should be installed separately with CUDA support, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
# The Dockerfile handles this automatically.

numpy
astunparse
torch>=2.1.0
transformers>=4.40.0
peft>=0.13.0
accelerate>=0.30.0
pandas
matplotlib
